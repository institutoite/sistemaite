use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

  
 $now = Carbon::now();
        $diff = $now->diffInSeconds("00:00 2024-04-03");
        $minutes = floor($diff / 60);
        $seconds = $diff % 60;
 $t=sprintf('%02d:%02d', $minutes, $seconds);
