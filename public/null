use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = "59:00";

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

        list($minutos, $segundos) = explode(':', $tiempoString);
        $tiempoCarbon = Carbon::today()->minute($minutos)->second($segundos);
        $minutes = $tiempoCarbon->minute;
        $seconds = $tiempoCarbon->second;
        sprintf('%02d:%02d', $minutes, $seconds);
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

$tiempoString;
      
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;



 $tiempoString = session('GContactTokenExpiration');


use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;



 $tiempoString = session('GContactTokenExpiration');


use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;



 $tiempoString = session('GContactTokenExpiration');


use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;



 $tiempoString = session('GContactTokenExpiration');


use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;



 $tiempoString = session('GContactTokenExpiration');


use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

$tiempoString;
      
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

$diferencia = $tiempoString->diffInSeconds(Carbon::now());
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

$diferencia = $tiempoString->diffInSeconds(Carbon::now());
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::now();

$diferencia = $tiempoString->diffInSeconds(Carbon::now());
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::parse('2024-04-11 04:00:00');

$diferencia = $tiempoString->diffInSeconds(Carbon::now());
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::parse('2024-04-11 03:00:00');

$diferencia = $tiempoString->diffInSeconds(Carbon::now());
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::parse('2024-04-11 03:00:00');

$diferencia = $tiempoString->diffInSeconds(Carbon::now());
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::parse('2024-04-11 6:00:00');

$diferencia = $tiempoString->diffInSeconds(Carbon::now());
use App\Models\Estudiante;
use App\Models\Persona;
use Carbon\Carbon;

$tiempoString = Carbon::parse('2024-04-11 01:00:00');

$diferencia = $tiempoString->diffInSeconds(Carbon::now());
