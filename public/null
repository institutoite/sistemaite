use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','programacions.inscripcione_id','inscripciones.id')
            ->join('programacions','clases.programacion_id','programacions.id')
            ->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.estudiante_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materia.id','clases.materia_id')
            ->where('estudiantes.id',$estudiante->id)
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            ->orderBy('fecha','asc')
            ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','programacions.inscripcione_id','inscripciones.id')
            ->join('programacions','clases.programacion_id','programacions.id')
            ->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.estudiante_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materia.id','clases.materia_id')
            ->where('estudiantes.id',1)
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            ->orderBy('fecha','asc')
            ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','clases.programacion_id','programacions.id')
            ->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.estudiante_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materia.id','clases.materia_id')
            ->where('estudiantes.id',1)
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            ->orderBy('fecha','asc')
            ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.estudiante_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materia.id','clases.materia_id')
            ->where('estudiantes.id',1)
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            ->orderBy('fecha','asc')
            ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('clases','clases.programacion_id','programacions.id')
  			->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.estudiante_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materia.id','clases.materia_id')
            ->where('estudiantes.id',1)
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            ->orderBy('fecha','asc')
            ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('clases','clases.programacion_id','programacions.id')
  			->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.estudiante_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materia.id','clases.materia_id')
            ->where('estudiantes.id',1)
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            ->orderBy('fecha','asc')
            ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('clases','clases.programacion_id','programacions.id')
  			->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.estudiante_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materia.id','clases.materia_id')
            ->where('estudiantes.id',1)
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            ->orderBy('fecha','asc')
            ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('clases','clases.programacion_id','programacions.id')
  			->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.persona_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materia.id','clases.materia_id')
            ->where('estudiantes.id',1)
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            ->orderBy('fecha','asc')
            ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('clases','clases.programacion_id','programacions.id')
  			->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.persona_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materias.id','clases.materia_id')
            ->where('estudiantes.id',1)
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            ->orderBy('fecha','asc')
            ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('clases','clases.programacion_id','programacions.id')
  			->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.persona_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materias.id','clases.materia_id')
            ->where('estudiantes.id',1)
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            
     ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::joi('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('clases','clases.programacion_id','programacions.id')
  			->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.persona_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materias.id','clases.materia_id')
            ->where('estudiantes.id',1)
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            
     ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('clases','clases.programacion_id','programacions.id')
  			->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.persona_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materias.id','clases.materia_id')
            ->where('estudiantes.id',1)
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            
     ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('clases','clases.programacion_id','programacions.id')
  			->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.persona_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materias.id','clases.materia_id')
            ->where('estudiantes.id',2)
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            
     ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('clases','clases.programacion_id','programacions.id')
  			->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.persona_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materias.id','clases.materia_id')
            ->where('estudiantes.id',1)
  			->where('clases.estado',estado('PRESENTE'))
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            
     ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('clases','clases.programacion_id','programacions.id')
  			->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.persona_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materias.id','clases.materia_id')
            ->where('estudiantes.id',1)
  			->where('clases.estado_id',estado('PRESENTE'))
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            
     ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('clases','clases.programacion_id','programacions.id')
  			->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.persona_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materias.id','clases.materia_id')
            
  			->where('clases.estado_id',estado('PRESENTE'))
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            
     ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('clases','clases.programacion_id','programacions.id')
  			->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.persona_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materias.id','clases.materia_id')
            ->where('docentes.id',1)
  			->where('clases.estado_id',estado('PRESENTE'))
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            
     ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$estudiantes=Estudiante::join('inscripciones','inscripciones.estudiante_id','estudiantes.id')
            ->join('programacions','programacions.inscripcione_id','inscripciones.id')
            ->join('clases','clases.programacion_id','programacions.id')
  			->join('docentes','docentes.id','clases.docente_id')
            ->join('personas','personas.id','estudiantes.persona_id')
            ->join('temas','temas.id','clases.tema_id')
            ->join('materias','materias.id','clases.materia_id')
            ->where('docentes.id',1)
  			->where('programacions.fecha','=',Carbon::now()->isoFormat('Y-M-D'))
  			->where('clases.estado_id',estado('PRESENTE'))
            ->select('personas.id','personas.nombre','personas.apellidop','personas.apellidom','temas.tema','materias.materia')
            
     ->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$docentes=Docente::get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;


$docentes=Docente::join("programacioncoms",'programacioncoms.docente_id','docentes.id')

->get();
