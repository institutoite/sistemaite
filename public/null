use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Clase::join('programacions','clases.programacion_id','programacions.id')
  		->join('inscripciones','programacions.inscripcione_id','inscripciones.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','clases.estado_id')
        ->where('inscripciones.id',31)
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Clase::join('programacions','clases.programacion_id','programacions.id')
  		->join('inscripciones','programacions.inscripcione_id','inscripciones.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','clases.estado_id')
        ->where('inscripciones.id',33)
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Clase::join('programacions','clases.programacion_id','programacions.id')
  		->join('inscripciones','programacions.inscripcione_id','inscripciones.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','clases.estado_id')
        ->where('inscripciones.id',31)
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Clase::join('programacions','clases.programacion_id','programacions.id')
  		->join('inscripciones','programacions.inscripcione_id','inscripciones.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','clases.estado_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','<',Carbon::now()->isoFormat("Y-M-D"))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Clase::join('programacions','clases.programacion_id','programacions.id')
  		->join('inscripciones','programacions.inscripcione_id','inscripciones.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','clases.estado_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','<=',Carbon::now()->isoFormat("Y-M-D"))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Clase::join('programacions','clases.programacion_id','programacions.id')
  		->join('inscripciones','programacions.inscripcione_id','inscripciones.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','clases.estado_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','<=',Carbon::now()->isoFormat("Y-M-D"))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Clase::join('programacions','clases.programacion_id','programacions.id')
  		->join('inscripciones','programacions.inscripcione_id','inscripciones.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','clases.estado_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','<=',Carbon::now()->isoFormat('Y-M-D'))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Programacion::join('clases','clases.programacion_id','programacions.id')
  		->join('inscripciones','programacions.inscripcione_id','inscripciones.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','clases.estado_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','<=',Carbon::now()->isoFormat('Y-M-D'))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Programacion::join('inscripciones','programacions.inscripcione_id','inscripciones.id')
		->join('clases','clases.programacion_id','programacions.id')
  		
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','clases.estado_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','<=',Carbon::now()->isoFormat('Y-M-D'))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Programacion::join('inscripciones','programacions.inscripcione_id','inscripciones.id')
		->join('clases','clases.programacion_id','programacions.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','clases.estado_id')
        ->where('inscripciones.id',33)
   		->where('programacions.fecha','<=',Carbon::now()->isoFormat('Y-M-D'))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Programacion::join('inscripciones','programacions.inscripcione_id','inscripciones.id')
		->join('clases','clases.programacion_id','programacions.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','clases.estado_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','<=',Carbon::now()->isoFormat('Y-M-D'))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Programacion::join('inscripciones','programacions.inscripcione_id','inscripciones.id')
		->join('clases','clases.programacion_id','programacions.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','clases.estado_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','>',Carbon::now()->isoFormat('Y-M-D'))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Programacion::join('inscripciones','programacions.inscripcione_id','inscripciones.id')
		->join('clases','clases.programacion_id','programacions.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','>',Carbon::now()->isoFormat('Y-M-D'))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Programacion::join('inscripciones','programacions.inscripcione_id','inscripciones.id')
		->join('clases','clases.programacion_id','programacions.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','<',Carbon::now()->isoFormat('Y-M-D'))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Programacion::join('inscripciones','programacions.inscripcione_id','inscripciones.id')
		->join('clases','clases.programacion_id','programacions.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','programacions.estado_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','>',Carbon::now()->isoFormat('Y-M-D'))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Programacion::join('inscripciones','programacions.inscripcione_id','inscripciones.id')
		->join('clases','clases.programacion_id','programacions.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','programacions.estado_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','<',Carbon::now()->isoFormat('Y-M-D'))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Programacion::join('inscripciones','programacions.inscripcione_id','inscripciones.id')
		
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->join('aulas','aulas.id','clases.aula_id')
  		->join('estados','estados.id','programacions.estado_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','<',Carbon::now()->isoFormat('Y-M-D'))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Programacion::join('inscripciones','programacions.inscripcione_id','inscripciones.id')
		
  		->join('docentes','docentes.id','programacions.docente_id')
  		->join('materias','materias.id','programacions.materia_id')
  		->join('temas','temas.id','programacions.tema_id')
  		->join('aulas','aulas.id','programacions.aula_id')
  		->join('estados','estados.id','programacions.estado_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','<',Carbon::now()->isoFormat('Y-M-D'))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Programacion::join('inscripciones','programacions.inscripcione_id','inscripciones.id')
		
  		->join('docentes','docentes.id','programacions.docente_id')
  		->join('materias','materias.id','programacions.materia_id')
  		
  		->join('aulas','aulas.id','programacions.aula_id')
  		->join('estados','estados.id','programacions.estado_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','<',Carbon::now()->isoFormat('Y-M-D'))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','temas.tema','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Programacion::join('inscripciones','programacions.inscripcione_id','inscripciones.id')
		
  		->join('docentes','docentes.id','programacions.docente_id')
  		->join('materias','materias.id','programacions.materia_id')
  		
  		->join('aulas','aulas.id','programacions.aula_id')
  		->join('estados','estados.id','programacions.estado_id')
        ->where('inscripciones.id',31)
   		->where('programacions.fecha','<',Carbon::now()->isoFormat('Y-M-D'))
  		->select('estados.estado','programacions.fecha','programacions.hora_ini','programacions.hora_fin','docentes.nombrecorto','materias.materia','aulas.aula')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Clase::join('programacions','clases.programacion_id','programacions.id')
  		->join('inscripciones','programacions.inscripcione_id','inscripciones.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
  		->select('clases.estado_id','clases.fecha','docentes.nombrecorto','materias.materia','temas.tema')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Clase::join('programacions','clases.programacion_id','programacions.id')
  		->join('inscripciones','programacions.inscripcione_id','inscripciones.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
   		->where('inscripcines.id',31)
  		->select('clases.estado_id','clases.fecha','docentes.nombrecorto','materias.materia','temas.tema')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Clase::join('programacions','clases.programacion_id','programacions.id')
  		->join('inscripciones','programacions.inscripcione_id','inscripciones.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
   		->where('inscripciones.id',31)
  		->select('clases.estado_id','clases.fecha','docentes.nombrecorto','materias.materia','temas.tema')
  		->orderBy('fecha','asc')
  		->get();
use App\Models\Estudiante;
use App\Models\Grado;
use App\Models\Docente;
use App\Models\Clase;
use App\Models\Programacion;
use Carbon\Carbon;





 $historia=Clase::join('programacions','clases.programacion_id','programacions.id')
  		->join('inscripciones','programacions.inscripcione_id','inscripciones.id')
  		->join('docentes','docentes.id','clases.docente_id')
  		->join('materias','materias.id','clases.materia_id')
  		->join('temas','temas.id','clases.tema_id')
   		->where('inscripciones.id',33)
  		->select('clases.estado_id','clases.fecha','docentes.nombrecorto','materias.materia','temas.tema')
  		->orderBy('fecha','asc')
  		->get();
